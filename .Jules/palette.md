## 2024-05-18 - Missing Tooltips on IconButtons
**Learning:** Found several `IconButton` widgets in this Flutter app that lacked tooltips, which are critical for screen readers and providing context on hover (e.g. 'Send message', 'Clear search').
**Action:** Always ensure `tooltip` properties are added to icon-only buttons to improve accessibility.
