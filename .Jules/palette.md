## 2024-06-14 - IconButton Tooltips
**Learning:** In Flutter, `IconButton` widgets without a `tooltip` property are inaccessible to screen readers. Many were missing across the app screens.
**Action:** Always add a descriptive `tooltip` property when using `IconButton` to ensure full accessibility.
