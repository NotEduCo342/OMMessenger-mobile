## 2024-05-24 - Add Tooltips to IconButtons
**Learning:** Icon-only buttons often lack tooltips, degrading accessibility for screen readers and masking action intent. This is a common pattern in this app.
**Action:** Always add `tooltip` properties to `IconButton` and other icon-only widgets to ensure accessibility and screen reader support.
