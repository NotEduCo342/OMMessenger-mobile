## 2024-05-24 - Accessibility improvements on IconButtons
**Learning:** Adding tooltips to `IconButton` widgets in Flutter provides essential semantic labels for screen readers and helpful hover text. It serves a similar function to `aria-label` in web development.
**Action:** Always verify if icon-only buttons have tooltips to ensure accessibility compliance.
