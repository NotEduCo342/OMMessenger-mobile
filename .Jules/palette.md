## 2024-06-13 - Added ARIA labels/tooltips to IconButtons
**Learning:** Found that `IconButton` widgets in this Flutter app often lacked `tooltip` attributes, making them inaccessible to screen readers and less intuitive for mouse users.
**Action:** Always ensure `IconButton` and other icon-only widgets have a descriptive `tooltip` attribute.
